#include <stdio.h>

int main() {
    int a, b, somme;

    // Demande de saisir deux entiers
    scanf("%d %d", &a, &b);

    // Calcul de la somme
    somme = a + b;

    // Affichage du résultat
    printf("La somme est : %d\n", somme);

    return 0;
}
